#include <iostream>void Interpret(std::string& line) {    std::cout << line << "\n";}int main() {    while (true) {        std::cout << "Twig> " << std::flush;        std::string line;        std::getline(std::cin, line);        if (std::cin.eof())            exit(1);        Interpret(line);    }}